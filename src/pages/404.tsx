import * as React from 'react';

// markup
const NotFoundPage = () => {
  return (
    <main>
      <p>I'm not sure what you are trying to do, but there's nothing here...</p>
    </main>
  );
};

export default NotFoundPage;
