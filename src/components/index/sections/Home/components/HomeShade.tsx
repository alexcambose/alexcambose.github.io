import Shade from '@/components/ui/Shade';
import styled from 'styled-components';

const HomeShade = styled(Shade)`
  left: 0;
  height: 150vh;
  clip-path: polygon(0 0, 100% 100%, 100% 0);
`;

export default HomeShade;
