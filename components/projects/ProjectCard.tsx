import { ProjectItem } from './types';

export interface ProjectCard {
  data: ProjectItem;
}
export const ProjectCard = ({ data }: ProjectCard) => {
  return (
    <div>
      <h3>aa</h3>
    </div>
  );
};
